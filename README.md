# pySimpleDB-QueryOptimizer
Implemented B-Tree and composite indexes, index-driven scans, selection pushdown, and heuristic join reordering to significantly improve query execution performance in pySimpleDB.
